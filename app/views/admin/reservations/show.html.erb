<h2 class="text-center mt-8 mb-6 text-xl font-bold">
  予約詳細（管理者）
</h2>

<div class="bg-gray-100 w-80 mx-auto p-4 rounded">
  <p>名前：<%= @reservation.user.name %></p>
  <p>電話：<%= @reservation.user.phone_number %></p>
  <p>体験：<%= @reservation.harvest_experience.title %></p>
  <p>人数：<%= @reservation.number_of_people %></p>
  <p>日時：<%= @reservation.reserved_at.strftime("%Y/%m/%d %H:%M") %></p>
</div>

<div class="text-center mt-6">
  <%= link_to "一覧へ戻る", admin_reservations_path,
      class: "bg-gray-200 hover:bg-gray-400 font-bold py-2 px-6 rounded border" %>
</div>